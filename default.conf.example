server {
    listen 80;
    server_name n8n.pharmaprice.shop;

    location / {
        return 301 https://$host$request_uri;
    }

    location /.well-known/acme-challenge/ {
        root /etc/nginx/webroot;
    }
}

server {
    listen 443 ssl;
    server_name n8n.pharmaprice.shop;

    ssl_certificate /etc/nginx/ssl/n8n-fullchain.pem;
    ssl_certificate_key /etc/nginx/ssl/n8n-privkey.pem;

    location / {
        proxy_pass http://n8n:5678;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}


server {
    listen 80;
    server_name evolution.pharmaprice.shop;

    location / {
        return 301 https://$host$request_uri;
    }

    location /.well-known/acme-challenge/ {
        root /etc/nginx/webroot;
    }
}

server {
    listen 443 ssl;
    server_name evolution.pharmaprice.shop;

    ssl_certificate /etc/nginx/ssl/evolution-fullchain.pem;
    ssl_certificate_key /etc/nginx/ssl/evolution-privkey.pem;

    location / {
        proxy_pass http://api:8080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

server {
    listen 80;
    server_name chatwoot.pharmaprice.shop;

    location / {
        return 301 https://$host$request_uri;
    }

    location /.well-known/acme-challenge/ {
        root /etc/nginx/webroot;
    }
}

server {
    listen 443 ssl;
    server_name chatwoot.pharmaprice.shop;

    ssl_certificate /etc/nginx/ssl/chatwoot-fullchain.pem;
    ssl_certificate_key /etc/nginx/ssl/chatwoot-privkey.pem;

    location / {
        proxy_pass http://chatwoot_rails:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}

